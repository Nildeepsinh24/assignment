# Q1: What is React.js and how is it different?

React.js is a JavaScript library for building user interfaces, mainly the front-end view layer.

Unlike full frameworks, React does not force one complete structure for routing, state, and APIs. You choose the supporting tools based on project needs. That flexibility is a major reason teams adopt React.
