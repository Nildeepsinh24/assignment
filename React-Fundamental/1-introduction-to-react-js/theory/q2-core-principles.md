# Q2: Core principles of React (Virtual DOM and Components)

React follows component-based architecture. A page is split into small reusable components, each handling its own UI and logic.

React also uses a Virtual DOM. It updates a lightweight copy first, compares changes, and then updates only the required parts in the real DOM. This improves rendering efficiency.
