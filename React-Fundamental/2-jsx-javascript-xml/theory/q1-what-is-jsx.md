# Q1: What is JSX and why is it used?

JSX is a syntax extension that lets us write HTML-like markup inside JavaScript.

It is used because UI structure becomes easier to read and maintain. You can see layout and logic together, which speeds up development.
