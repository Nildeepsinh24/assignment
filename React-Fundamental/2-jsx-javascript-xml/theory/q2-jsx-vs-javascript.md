# Q2: How is JSX different from regular JavaScript?

JSX is not plain HTML. During build time, it is converted into JavaScript function calls.

Yes, JavaScript can be written inside JSX by using curly braces `{}`.
