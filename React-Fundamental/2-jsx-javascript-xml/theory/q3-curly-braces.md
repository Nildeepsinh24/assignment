# Q3: Why are curly braces important in JSX?

Curly braces tell JSX to evaluate JavaScript expressions.

They are used for dynamic values like variables, function results, and conditional expressions. Without braces, content is treated as static text.
