# Q1: What are components? Functional vs class components

Components are reusable building blocks of a React UI.

Functional components are plain JavaScript functions that return JSX and are the modern default.

Class components use ES6 classes and a `render()` method. They are still valid but less common in new React projects.
