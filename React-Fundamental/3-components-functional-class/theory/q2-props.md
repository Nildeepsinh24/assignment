# Q2: How do you pass data using props?

Props are values passed from parent to child components using attributes.

Example:

```jsx
<Greeting name="Neel" />
```

The child reads `name` from props and renders it.
