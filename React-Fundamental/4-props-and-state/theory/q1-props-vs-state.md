# Q1: What are props? How are props and state different?

Props are read-only inputs given by a parent component.

State is internal data controlled by the component itself.

In short: props come from outside, state is managed inside.
