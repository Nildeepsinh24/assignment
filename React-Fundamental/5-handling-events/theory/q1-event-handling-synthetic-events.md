# Q1: Event handling in React and synthetic events

React events are attached in JSX using camelCase, such as `onClick` and `onChange`.

React provides Synthetic Events, which are wrappers around native events to keep behavior consistent across browsers.
