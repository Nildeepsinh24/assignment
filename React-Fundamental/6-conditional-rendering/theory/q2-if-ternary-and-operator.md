# Q2: if-else, ternary, and && in JSX

- `if-else`: best when you need bigger logic before returning JSX.
- Ternary `condition ? A : B`: best for inline two-way choices.
- `&&`: best when rendering something only if a condition is true.

All three are valid; choose the one that keeps code readable.
